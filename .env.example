# URL pública del sitio, sin barra final. En Vercel usa la URL del deployment
# o el dominio propio una vez configurado (Project Settings → Domains).
NEXT_PUBLIC_SITE_URL=https://iansolis.com

# Usuario de Instagram (sin @). Déjalo vacío mientras no exista uno
# confirmado — el enlace se omite automáticamente, nunca se rompe.
NEXT_PUBLIC_INSTAGRAM=
